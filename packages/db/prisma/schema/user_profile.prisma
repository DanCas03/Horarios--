// =============================================================================
// Composite Types (Embedded Documents)
// =============================================================================

type ApprovedSubjectPeriod {
  code     String?
  start    DateTime?
  end      DateTime?
  termType String? @map("term_type")
}

type ApprovedSubjectItem {
  subjectId String?               @map("subject_id") @db.ObjectId
  grade     Float?
  period    ApprovedSubjectPeriod?
}

// =============================================================================
// User Profile
// =============================================================================
model UserProfile {
  id                   String                @id @default(auto()) @map("_id") @db.ObjectId
  userId               String?               @unique @map("user_id")
  user                 User?                 @relation(fields: [userId], references: [id], onDelete: Cascade)
  universityIds        String[]              @map("university_ids")
  academicProgramIds   String[]              @map("academic_program_ids") @db.ObjectId
  approvedSubjects     ApprovedSubjectItem[] @map("approved_subjects")
  totalApprovedCredits Int                   @default(0) @map("total_approved_credits")
  surveyCompleted      Boolean               @default(false) @map("survey_completed")
  createdAt            DateTime              @default(now()) @map("created_at")
  updatedAt            DateTime              @updatedAt @map("updated_at")
  // Mentions the student is currently enrolled in (capped by AcademicProgram.maxMentions)
  activeMentionIds     String[]              @map("active_mention_ids") @db.ObjectId

  @@map("user_profiles")
}
